# Home System Configuration
# Command aliases are now managed in aliases.yml

# Default maximum number of homes (used if no permission is set)
# NOTE: This limit applies even with OP status, can only be increased via permission
default-max-homes: 3

# Teleportation animation type (standard / gta_style)
teleport-animation: 'gta_style'

# During GTA style animation, blindness effect is applied if you are
# further than this distance (blocks) from the target location.
gta-style-blindness-distance: 100.0

# Teleportation delay time (seconds)
teleport-delay-seconds: 3

# Cancel teleportation when moving?
cancel-on-move: true

# Cancel teleportation when taking or dealing damage?
cancel-on-damage: true

# Cancel teleportation when breaking or placing blocks?
cancel-on-block-break: true

# Teleportation effect settings
effects:
  # Show particle effects
  show-particles: true

  # Radius of the ring that appears when teleportation completes
  teleport-particle:
    radius: 2.0

# Banned worlds - Players cannot set homes in these worlds
banned-worlds:
  - "world_nether"
  - "world_the_end"

# Menu settings
menu:
  # Show home location (world, x, y, z) in home menu
  show-location: true

# Do not touch
version: 4
